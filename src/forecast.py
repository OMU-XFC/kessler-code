def forecast(asteroid_positions, asteroid_velocity, time_steps):
    """
    Given a list of asteroid positions and their velocity, predict their future positions.
    :param asteroid_positions: An (n, 2) array of the asteroids (x, y) coordinates
    :param asteroid_velocity: An (n, 2) array of the asteroids (vx, vy) speeds
    :param time_steps:
    :return:
    """
    pass
